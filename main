
#include <iostream>

using namespace std;

int main() {
    int n;
    cout << "Enter a number: ";
    cin >> n;

    if (n < 1) {
        cout << "Please enter a number greater than 0." << endl;
        return 1;
    }

    cout << "Odd numbers from 1 to " << n << " are: ";
    for (int i = 1; i <= n; ++i) {
        if (i % 2 != 0) {
            cout << i << " ";
        }
    }
    cout << endl;

    return 0;
}

